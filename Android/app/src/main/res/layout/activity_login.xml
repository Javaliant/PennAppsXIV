<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:background="@color/colorPrimaryLight"
    tools:context="com.luigivincent.pennappsxiv.LoginActivity">

    <EditText
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/username_field"
        android:layout_alignParentTop="true"
        android:layout_marginTop="147dp"
        android:layout_alignEnd="@+id/password_field"
        android:layout_alignStart="@+id/password_field"
        android:inputType="text"
        android:textColor="@color/colorText"
        android:textColorHint="@color/colorTextContrast"
        android:hint="@string/username"/>

    <EditText
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:inputType="numberPassword"
        android:ems="10"
        android:id="@+id/password_field"
        android:layout_centerVertical="true"
        android:layout_centerHorizontal="true"
        android:textColor="@color/colorText"
        android:textColorHint="@color/colorTextContrast"
        android:hint="@string/password" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/sign_in"
        android:id="@+id/sign_in_button"
        android:layout_below="@+id/password_field"
        android:layout_marginTop="53dp"
        android:layout_alignEnd="@+id/password_field"
        android:background="@color/colorTextContrast"
        android:textColor="@color/colorPrimary"
       android:layout_alignStart="@+id/password_field" />

    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/logo"
        android:contentDescription="@string/penn_apps_logo"
        android:src="@drawable/penn_apps"
        android:layout_marginBottom="98dp"
        android:layout_alignBottom="@+id/username_field"
        android:layout_alignParentStart="true" />

</RelativeLayout>
